Order Management Service — Kotlin / Spring Boot / PostgreSQL / Kafka / Redis

Проект представляет собой сервис управления заказами, построенный на Kotlin и Spring Boot.
В архитектуре используются технологии:

PostgreSQL для хранения данных

Kafka (Producer + Consumer) для обработки событий и асинхронных процессов

Redis для кеширования и хранения refresh-токенов

Spring Security + JWT для аутентификации

Пагинация двух видов:
классическая (pageNumber, pageSize)
key-based (lastSeenId) для работы с большими таблицами
Также реализован поиск пользователей и продуктов через Query Builder, Swagger/OpenAPI документация и разделение слоёв (Controller, Service, Repository).

📦 О сервисе заказов 
Сервис заказов отвечает за создание, получение и управление заказами пользователей.
Функционал включает:
создание заказа пользователем
получение списка заказов с пагинацией
просмотр заказа по ID
Kafka-уведомления при создании заказа
кеширование данных заказов в Redis для ускорения повторных запросов
безопасная работа через JWT и refresh-токены
